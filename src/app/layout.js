import "./globals.css";

export const metadata = {
    title: "BooleanBuffer",
    description: "BooleanBuffer",
};

export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <body>{children}</body>
        </html>
    );
}
